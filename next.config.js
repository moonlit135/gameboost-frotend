/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'api.qrserver.com',
      'images.unsplash.com',
      'plus.unsplash.com',
      'source.unsplash.com'
    ],
  },
};

module.exports = nextConfig;
